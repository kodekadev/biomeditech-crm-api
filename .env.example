PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

GCP_PROJECT_ID=jobs-425301
BIGQUERY_DATASET=CRM
BIGQUERY_LOCATION=us-central1

# Token dedicado para proveedores externos que envian leads.
# Debe enviarse como Authorization: Bearer <token> o x-api-key.
LEADS_INGEST_TOKEN=change-me-generate-a-long-random-token

# Local development only. In Cloud Run, prefer Workload Identity / service account.
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json
